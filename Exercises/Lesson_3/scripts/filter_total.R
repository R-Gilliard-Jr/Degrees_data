filter_total <- function(data) {
  data <- data %>%
    # The correct column to filter on is Total.Student.Response.Rate
    [your code goes here]
  return(data)
}
# Remember, once you're done coding, Save and submit() your code.