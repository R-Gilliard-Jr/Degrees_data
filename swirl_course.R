# Set path
setwd("D:/Documents/Work/DegreesNYC/Degrees_data")

# Load packages
pkgs <- c("swirlify")
lapply(pkgs, library, character.only = T)

# Lesson 1
new_lesson("Lesson 1", "Exercises")

set_lesson()
